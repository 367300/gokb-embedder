# GoKB Embedder

Универсальный Go-скрипт для создания эмбедингов из кодовой базы проекта с поддержкой умного обновления и игнорирования файлов по `.gitignore`.

## 🎯 Возможности

- **Парсинг Python файлов** — извлечение методов и функций с метаинформацией
- **Обработка документации** — разбивка `.md`, `.yml`, `.conf` файлов на блоки
- **История коммитов** — добавление последних сообщений коммитов к каждому блоку
- **Умное обновление** — пересоздание эмбедингов только для изменённых файлов
- **Игнорирование по .gitignore** — автоматическое исключение файлов и папок
- **Модульная архитектура** — легко расширяемая система парсеров
- **Высокая производительность** — оптимизированная обработка больших проектов

## 📋 Требования

- Go 1.21+
- SQLite3
- OpenAI API ключ

## ⚙️ Установка

```bash
# Клонируем репозиторий
git clone <repository-url>
cd gokb-embedder

# Устанавливаем зависимости
go mod tidy

# Собираем приложение
go build -o gokb-embedder cmd/main.go
```

## 🔧 Настройка

### 1. Создайте файл `.env`:
```env
OPENAI_API_KEY=your_openai_api_key_here
ROOT_DIR=.
FILE_EXTENSIONS=.py,.md,.yml,.conf
DB_PATH=embeddings.sqlite3
N_COMMITS=3
TOKEN_LIMIT=1600
LOG_LEVEL=info
```

### 2. Переменные окружения:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `OPENAI_API_KEY` | Ключ OpenAI API (обязательно) | - |
| `ROOT_DIR` | Корневая директория для поиска файлов | `.` |
| `FILE_EXTENSIONS` | Расширения файлов для обработки | `.py,.md,.yml,.conf` |
| `DB_PATH` | Путь к файлу базы данных | `embeddings.sqlite3` |
| `N_COMMITS` | Количество последних коммитов | `3` |
| `TOKEN_LIMIT` | Лимит токенов на блок | `1600` |
| `LOG_LEVEL` | Уровень логирования | `info` |

## 🚀 Использование

```bash
# Запуск с настройками по умолчанию
./gokb-embedder

# Запуск с кастомными настройками
OPENAI_API_KEY=your_key ROOT_DIR=/path/to/project ./gokb-embedder
```

## 🏗️ Архитектура проекта

```
gokb-embedder/
├── cmd/
│   └── main.go              # Точка входа приложения
├── internal/
│   ├── app/                 # Основная логика приложения
│   │   └── app.go
│   ├── config/              # Конфигурация
│   │   ├── config.go
│   │   └── errors.go
│   ├── database/            # Работа с базой данных
│   │   └── database.go
│   ├── git/                 # Работа с Git
│   │   └── git.go
│   ├── models/              # Модели данных
│   │   └── codeblock.go
│   ├── openai/              # OpenAI API клиент
│   │   └── openai.go
│   ├── parsers/             # Парсеры файлов
│   │   ├── parser.go        # Интерфейс парсера
│   │   ├── python_parser.go # Парсер Python
│   │   └── text_parser.go   # Парсер текстовых файлов
│   ├── scanner/             # Сканирование файлов
│   │   └── scanner.go
│   └── utils/               # Утилиты
│       └── tokenizer.go
├── go.mod                   # Зависимости Go
├── go.sum                   # Хеши зависимостей
└── README.md               # Документация
```

## 📊 Структура базы данных

### Таблица `embeddings`:
- `id` — уникальный идентификатор
- `embedding` — вектор эмбединга (JSON)
- `file_path` — путь к файлу
- `block_type` — тип блока (`method`, `function`, `markdown`, `yaml`, `config`)
- `class_name` — имя класса (для методов)
- `method_name` — имя метода/функции
- `start_line`, `end_line` — диапазон строк
- `commit_messages` — сообщения коммитов (JSON)
- `raw_text` — исходный текст блока
- `embedding_text` — полный текст для эмбединга
- `created_at` — время создания

### Таблица `file_hashes`:
- `file_path` — путь к файлу (PRIMARY KEY)
- `file_hash` — MD5-хеш файла для отслеживания изменений
- `updated_at` — время обновления

## 🔍 Логика работы

### 1. Парсинг Python файлов
- Извлекает методы классов с комментарием о принадлежности к классу
- Извлекает функции вне классов
- Сохраняет метаинформацию (имя класса, метод, строки)

### 2. Обработка текстовых файлов
- Разбивает `.md`, `.yml`, `.conf` на блоки по токенам
- Сохраняет диапазон строк для каждого блока

### 3. Получение истории коммитов
- Для каждого файла получает последние коммиты
- Добавляет сообщения коммитов к каждому блоку

### 4. Формирование текста для эмбединга
Полный текст, отправляемый в OpenAI API:

```
File: django_base/views.py
Class: UserView
Method/Function: get_user
Lines: 15-30
Recent commits: Add user validation; Fix authentication bug; Update docs

Code:
def get_user(self):
    # class: UserView
    return self.user
```

### 5. Умное обновление
- Вычисляет MD5-хеш каждого файла
- Сравнивает с сохранёнными хешами
- Обновляет только изменённые файлы
- Удаляет старые блоки перед добавлением новых

## 🚫 Игнорирование файлов

Скрипт автоматически читает `.gitignore` и игнорирует:
- Файлы и папки по паттернам `.gitignore`
- Поддиректории игнорируемых папок
- Файлы с указанными расширениями

## 🔧 Расширение функциональности

### Добавление нового парсера

1. Создайте новый файл в `internal/parsers/`
2. Реализуйте интерфейс `Parser`
3. Зарегистрируйте парсер в `app.registerParsers()`

Пример:

```go
// internal/parsers/go_parser.go
type GoParser struct{}

func (gp *GoParser) GetName() string {
    return "go"
}

func (gp *GoParser) CanParse(fileExtension string) bool {
    return fileExtension == ".go"
}

func (gp *GoParser) ParseFile(filePath string) ([]*models.CodeBlock, error) {
    // Реализация парсинга Go файлов
}
```

### Добавление в app.go:

```go
func (r *App) registerParsers() {
    // ... существующие парсеры ...
    
    // Регистрируем Go парсер
    goParser := parsers.NewGoParser()
    r.parsers.Register(goParser)
}
```

## 📝 Примеры использования

### Поиск по базе знаний:
```sql
-- Найти все методы класса UserView
SELECT * FROM embeddings WHERE class_name = 'UserView';

-- Найти функции с упоминанием "authentication"
SELECT * FROM embeddings WHERE raw_text LIKE '%authentication%';

-- Найти блоки из последних коммитов
SELECT * FROM embeddings WHERE commit_messages LIKE '%bug fix%';

-- Просмотреть полный текст эмбединга для отладки
SELECT embedding_text FROM embeddings WHERE id = 1;
```

## 🐛 Устранение неполадок

### Ошибка "no such table: file_hashes"
- Скрипт автоматически создаст недостающую таблицу
- Если проблема повторяется, удалите файл БД и запустите заново

### Файлы не игнорируются
- Проверьте синтаксис `.gitignore`
- Убедитесь, что файл `.gitignore` находится в корне проекта

### Ошибки OpenAI API
- Проверьте правильность API ключа в `.env`
- Убедитесь в наличии средств на аккаунте OpenAI

### Ошибки Git
- Убедитесь, что проект является Git репозиторием
- Проверьте права доступа к `.git` директории

## 📈 Производительность

- **Умное обновление** — только изменённые файлы
- **Прогресс-бары** для отслеживания процесса
- **Обработка ошибок** — продолжение работы при проблемах с отдельными файлами
- **Оптимизированная работа с БД** — пакетные операции

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.
